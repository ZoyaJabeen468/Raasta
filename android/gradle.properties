org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Project is on E: and Pub cache is on C:. Kotlin incremental caches
# cannot relativize paths across drive letters.
kotlin.incremental=false
# tflite_flutter compiles Java 11 while Kotlin defaults to the JDK (21).
kotlin.jvm.target.validation.mode=warning
